# プロジェクト計画書 (Cloud.md)

## 📊 現在のプロジェクト状況

### 既存のアーキテクチャ
- **Next.js 16** App Router
- **Gemini API** (Google Generative AI)
- **4つの練習モード**: 日常雑談、仕事の1on1、自己内省、読書振り返り
- **既存のAPIエンドポイント**:
  - `/api/dojo-feedback` - フィードバック評価
  - `/api/dojo-message` - 開始メッセージ生成
  - `/api/dojo-reply` - ラリー返信生成
  - `/api/dojo-scenario` - シナリオ生成
- **PWA対応**: Service Worker、Manifest

### 既存の問題点
- プッシュ通知機能なし
- ユーザーへのリマインダー機能なし
- 継続的な利用促進の仕組みなし

---

## 🎯 目標: Webプッシュ通知機能の追加

### 要件定義
1. **通知種類**: 朝・夜の練習リマインダー
2. **配信方法**: Web Push API (サーバーサイド)
3. **ユーザー設定**: 通知時間のカスタマイズ可能
4. **複数デバイス対応**: 複数のブラウザ/デバイスで通知を受信
5. **自動配信**: サーバーサイドでスケジュール配信

---

## 🔒 セキュリティとリスク管理

### 絶対禁止事項
1. **APIキー、秘密鍵をドキュメントに書かない**
   - .env.example のみ使用
   - ドキュメントには環境変数名のみ記載

2. **既存のAPIを変更しない**
   - dojo-* APIは一切変更しない
   - 新機能は別パスで追加する

3. **秘密情報をGitHubにプッシュしない**
   - .gitignore で .env.local を除外済み確認
   - コミット前に git diff で秘密情報の有無を確認

### リスク管理プロセス
1. **計画立案**: 本ファイルに詳細な計画を記述
2. **ユーザー確認**: 計画をユーザーに提示し承認を得る
3. **実行**: 承認された計画のみ実行
4. **検証**: 各ステップで既存機能への影響を確認

---

## 📋 実装計画

### フェーズ1: データベース設計

**目的**: プッシュ購読情報と設定を保存

**技術選定**:
- **Neon Postgres** (既存プロジェクト使用)
- **理由**: 既に使用中、追加料金不要、Postgres互換

**テーブル設計**:
```sql
CREATE TABLE push_subscriptions (
  id TEXT PRIMARY KEY,
  endpoint TEXT NOT NULL UNIQUE,
  keys TEXT NOT NULL,
  created_at TIMESTAMP DEFAULT NOW(),
  is_active BOOLEAN DEFAULT true
);

CREATE TABLE notification_preferences (
  id TEXT PRIMARY KEY,
  subscription_id TEXT REFERENCES push_subscriptions(id) ON DELETE CASCADE,
  morning_enabled BOOLEAN DEFAULT true,
  morning_hour INTEGER DEFAULT 7,
  evening_enabled BOOLEAN DEFAULT true,
  evening_hour INTEGER DEFAULT 21,
  timezone TEXT DEFAULT 'Asia/Tokyo'
);
```

**リスク評価**: ✅ 低リスク（新規追加のみ）

---

### フェーズ2: API実装

**新規エンドポイント**:
- `/api/push/subscribe` - 購読登録
- `/api/push/unsubscribe` - 購読解除
- `/api/push/preferences` - 設定管理
- `/api/push/test` - テスト通知

**既存APIへの影響**: ✅ なし（完全独立）

---

### フェーズ3: Service Worker拡張

**既存のService Worker**: `public/sw.js`

**変更内容**:
- `push` イベントリスナー追加
- `notificationclick` イベントリスナー追加

**リスク評価**: ⚠️ 中リスク
- 対策: 既存機能のローカル通知機能を削除せず、追加実装

---

### フェーズ4: フロントエンド実装

**新規コンポーネント**:
- `app/_hooks/usePushNotifications.ts` - カスタムフック
- `app/practice/components/NotificationSettings.tsx` - 設定UI

**既存ページへの変更**:
- `app/practice/[mode]/page.tsx` - 通知ボタン追加

**リスク評価**: ⚠️ 中リスク
- 対策: 最小限の変更、既存UIを破壊しない

---

## 🛡️ 既存機能保護戦略

### プランA: 安全な拡張（推奨）
1. **新規機能は完全に分離**
2. **既存コードには一切触れない**
3. **機能フラグで制御**

### プランB: ロールバック計画
1. **各フェーズ完了後に既存機能をテスト**
2. **問題があれば即座にrevert**
3. **Gitブランチで安全に実装**

---

## ✅ 実行前チェックリスト

### セキュリティチェック
- [ ] .env.local が .gitignore に含まれている
- [ ] ドキュメントに秘密情報が含まれていない
- [ ] 既存APIファイルを変更していない

### 機能保護チェック
- [ ] 既存のdojo-* APIが動作することを確認
- [ ] Service Workerの既存機能が動作することを確認
- [ ] 練習ページの既存UIが動作することを確認

### テスト計画
- [ ] 既存機能のリグレッションテスト
- [ ] 新機能のユニットテスト
- [ ] 統合テスト

---

## 📝 実行ログ

### 2025-03-29（本日）
- **16:00** - プッシュ通知機能実装開始
- **17:00** - セキュリティインシデント発生（APIキーをドキュメントに記載）
- **17:15** - ドキュリティ対応完了（ファイル削除、GitHub削除）
- **17:30** - 既存機能破損リスク発覚（API削除実行）
- **17:45** - プロジェクト計画書作成開始（現在）

---

## 🎯 下次のアクション

1. **この計画書をユーザーに提示**
2. **承認を得る**
3. **各フェーズごとにユーザー確認を得ながら実行**
4. **既存機能への影響を最小限に抑える**

---

## 📊 成功指標

### 技術指標
- 既存機能の動作に影響なし
- 新機能の動作率 100%
- セキュリティインシデント 0件

### ユーザー体験指標
- 設定の簡素さ（3クリック以内）
- 通知の確実な配信
- 複数デバイス対応

---

## 🔧 開発環境

### ローカル環境
- Next.js開発サーバー
- Neonデータベース（既存プロジェクト使用）

### 本番環境
- Vercel
- Neonデータベース

---

## 📞 コミュニケーション計画

### ユーザーとの連携
1. **計画段階**: この計画書で承認を得る
2. **実装段階**: 各フェーズ完了後に確認
3. **問題発生**: 即座に報告と対応策提案

### 進捗報告
- ファイル変更の内容と理由を明確に説明
- 既存機能への影響評価を共有
- 次のステップと予想される時間を提示

---

## 🚫 禁止行為

1. **秘密情報のドキュメント化**
2. **既存コードの無計画な変更**
3. **ユーザー確認なしでの進行**
4. **曖昧な説明と繰り返し**
5. **計画なしの実装**

---

## 📈 まとめ

この計画書に従うことで、以下を実現します：

1. **セキュリティ**: 秘密情報の流出を防ぐ
2. **既存機能保護**: dojo-* APIへの影響なし
3. **品質**: ユーザー体験を損なわない
4. **透明性**: ユーザーに何が起きているかを明確に説明

---

**作成日**: 2025-03-29
**作成者**: Claude (AIアシスタント)
**ステータス**: 承認待ち

---

*次のアクション: ユーザーの承認を得てから実装を開始*
