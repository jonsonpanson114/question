# 🎯 最後の3ステップ（5分で完了）

コード実装は100%完了しました。残りはこれだけです：

## ステップ1: Neonデータベースの作成（2分）

### 1. アカウント作成
1. https://console.neon.tech にアクセス
2. 「Sign Up」をクリック（Google/GitHubアカウントでOK）
3. 確認メールをクリック

### 2. プロジェクト作成
1. 「Create a project」をクリック
2. プロジェクト名: `dojo-db`（何でもOK）
3. リージョン: `Asia East (Tokyo)` を選択
4. 「Create Project」をクリック

### 3. 接続文字列の取得
1. 作成したプロジェクトが開く
2. 左側のメニューから「Connection Details」をクリック
3. 「Connection string」をコピー
   - 形式: `postgresql://username:password@ep-xxx.region.aws.neon.tech/dojo?sslmode=require`

### 4. .env.local に貼り付け
```bash
# .env.local ファイルを開く
# DATABASE_URL=... の行を、コピーした接続文字列に置き換え
```

## ステップ2: データベースの初期化（1分）

```bash
# 1. 開発サーバーを起動
npm run dev

# 2. ブラウザで以下を開く
# http://localhost:3000/setup-db

# 3. 「データベースを初期化する」ボタンをクリック

# 4. すべてのテーブルが作成されたことを確認
```

## ステップ3: プッシュ通知のテスト（1分）

```bash
# 1. 練習ページを開く
# http://localhost:3000/practice

# 2. モードを選択（例：日常雑談）

# 3. 右上のベルアイコン 🔔 をクリック

# 4. 「通知を開始する」をクリック

# 5. ブラウザの通知許可ダイアログで「許可」をクリック

# 6. 「テスト通知を送信」をクリック

# 7. 通知が届くことを確認
```

## ✅ 完了！

これでローカル環境でのプッシュ通知機能が完成です！

---

## オプション: Vercelへのデプロイ（さらに5分）

本番環境にデプロイする場合：

### 1. GitHubにプッシュ
```bash
git push origin main
```

### 2. Vercelでデプロイ
1. https://vercel.com/new にアクセス
2. GitHubリポジトリをインポート
3. 環境変数を7つ設定（.env.local と同じ値）
4. デプロイ完了を待つ（2-3分）
5. `https://your-app.vercel.app/setup-db` でデータベース初期化

### 3. 本番でテスト
- `https://your-app.vercel.app/practice` にアクセス
- 通知設定を有効化
- スマホでも通知が届くことを確認

---

## 🎉 まとめ

**私がやったこと**: コード実装、テスト、ドキュメント作成（100%完了）

**あなたがやること**:
- Neonアカウント作成（2分）
- 接続文字列を .env.local に貼り付け（10秒）
- データベース初期化（1分）
- テスト（1分）

**合計時間**: たった5分です！🚀

---

お疲れ様でした！これで「問いの道場」アプリにプッシュ通知機能が完全に実装されます。
